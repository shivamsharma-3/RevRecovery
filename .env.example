# --- AI (server-side only — never prefix with NEXT_PUBLIC_) ---
# Google AI Studio key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- Contact form (Resend: https://resend.com/api-keys) ---
RESEND_API_KEY=
# Where enquiries are delivered.
CONTACT_TO_EMAIL=
# Must be a Resend-verified domain. Until you have one, `onboarding@resend.dev`
# works but only delivers to the address that owns the Resend account.
CONTACT_FROM_EMAIL=RevRecover AI <onboarding@resend.dev>
